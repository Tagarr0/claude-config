# rules/

Instructions scoped to part of this repo, via `paths:` frontmatter.
A rule with `paths: ["panels/**"]` only enters context when Claude touches that
folder. This is the pressure valve for a CLAUDE.md growing past ~200 lines.
